{
  "name": "news",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test"
  },
  "dependencies": {
    "jpeg-js": "^0.4.4"
  }
}
